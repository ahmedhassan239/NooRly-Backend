<IfModule mod_rewrite.c>
    RewriteEngine On

    # If backend root is used as DocumentRoot, forward all traffic to /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
